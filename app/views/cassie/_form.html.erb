<%= form_for(@endpoint, :url => url_path, html: { class: "form-horizontal" }) do |f| %>
  <% if @endpoint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@endpoint.errors.count, "error") %> prohibited this endpoint from being saved:</h2>
      <ul>
      <% @endpoint.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="control-group">
      <label class="control-label" for="inputURLl">Endpoint</label>
      <div class="controls">
        <%= f.text_field :endpoint, :value => @endpoint.endpoint %>
      </div>
    </div>
    <div class="control-group">
      <div class="controls">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    </div>

<% end %>
